language: perl
perl:
    - "5.16"
env: PERL_CPANM_OPT="--notest --force --skip-satisfied"
before_install:
    - "cpanm Carton"
install: 
    - "carton install 2>&1 | sed -u 's/^/       /'"
script:
    - "carton exec -- prove t"
